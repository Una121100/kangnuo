.tabbar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  background:#fff;
  border-top: 1px solid #eee;
}
.tab {
  flex: 1;
  text-align: center;
  padding: 12px 0;
  color: var(--muted);
}
.active {
  color: var(--brand);
  font-weight: 600;
}
